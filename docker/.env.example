# Instance Configuration
INSTANCE_NAME=wallboard
APP_PORT=8080

# Database Configuration
DB_NAME=wallboard
DB_USER=wallboard
DB_PASS=change_me_to_secure_password
DB_ROOT_PASS=change_me_to_secure_root_password
DB_PORT=3306

# Timezone
TIMEZONE=America/Chicago

# AMI Configuration (set via admin GUI after install)
# AMI_HOST=pbx.example.com
# AMI_PORT=5038
# AMI_USER=wallboard
# AMI_PASS=secret
